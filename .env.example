# Public build-time configuration exposed to the Vite client bundle.
# Copy this file to .env.local for local development.
# Vite embeds this absolute URL into the client bundle during build.
VITE_CTA_URL=https://example.com/waitlist
